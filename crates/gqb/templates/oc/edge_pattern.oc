{% if let Some(path) = path_variable %}{{ path }} = {% endif %}({{ source }})-[{{ var }}{% if !labels.is_empty() %}:{{ labels|join(":") }}{% endif %}{% if has_properties %} {{ properties_binding }}{% endif %}]->({{ destination }})